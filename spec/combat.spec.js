var common = require('../common');
var Player = require('../player');
var Entity = require('../entity');
var Combat = require('../combat');
var expect = common.expect;

describe("Combat should return a results object", function() {
    it("returns an object with the damage done and received", function() {

    });
    it("returns an object with a flag for slaying the enemy", function() {

    });
    it("returns an object with a flag when you are killed", function() {

    });
});